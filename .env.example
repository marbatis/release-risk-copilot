# Leave OPENAI_API_KEY unset to force deterministic mock mode.
OPENAI_API_KEY=
# Optional model for explanation provider.
OPENAI_MODEL=gpt-5-mini
# SQLite default is fine for local demos.
DATABASE_URL=sqlite:///./release_risk.db
# Upload size limit in bytes for JSON upload endpoint.
UPLOAD_MAX_BYTES=200000
